# Autonomous Agent Directives

- **Automatic Execution**: Always create, modify, build, test, and push code files automatically without asking for file-by-file confirmation.
- **Continuous Quality Assurance**: Ensure all 3 builds (`backend/`, `NurseryEcommerce/`, `admin/`) compile cleanly before pushing to GitHub (`origin main`).
